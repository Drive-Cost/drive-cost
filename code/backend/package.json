{
    "name": "drivecost-backend",
    "private": true,
    "version": "0.1.0",
    "engines": {
        "node": ">=20"
    },
    "description": "Offline-first backend module for DriveCost",
    "scripts": {
        "dev": "tsx src/server.ts",
        "build": "npm --prefix ../contracts run build && tsc -p tsconfig.json",
        "start": "node dist/server.js",
        "db:migrate": "tsx src/platform/postgres/migrate.ts",
        "typecheck": "npm --prefix ../contracts run build && tsc --noEmit -p tsconfig.json && tsc -p tsconfig.test.json",
        "test": "npm --prefix ../contracts run build && DRIVECOST_DATA_DIR=.test-data node --import tsx --test 'test/**/*.test.ts'",
        "test:postgres": "npm --prefix ../contracts run build && npm run db:migrate && node --import tsx --test test/modules/auth/routes.postgres.test.ts"
    },
    "dependencies": {
        "@drivecost/contracts": "file:../contracts",
        "@fastify/jwt": "^10.2.0",
        "fastify": "^5.2.1",
        "postgres": "^3.4.9"
    },
    "devDependencies": {
        "@types/node": "^24.0.0",
        "tsx": "^4.20.0",
        "typescript": "^5.8.0"
    }
}
